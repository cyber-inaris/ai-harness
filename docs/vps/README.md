# VPS

Runbooks for provisioning and operating the Linux VPS that hosts the agent/router stack.

Planned topics:

- Ubuntu Server baseline.
- XFCE + xrdp remote desktop.
- Cockpit server admin UI.
- SSH tunnels.
- Firewall rules.
- Docker installation.

